# Website Status Checker

A simple Python script that monitors a website’s HTTP status code at regular intervals.

## How to use

1. Install dependencies:

pip install -r requirements.txt

2. Run the script:

python status_checker.py

## Dependencies

- requests